#include <cmath>                                     //mathematica lib
double fun(double x)
{
	double y = 4 * pow(x, 3) - 8 * pow(x, 2) + 2 * x + 1;
	return y;
}



//eg:

//double y = f(x);
//double y = (x - 4) * (x + 1);


//C++              math
//exp(x)           e ^ x;
//pow(x, *)        x ^ *;
//cos(x)           cosx;
//sin(x)           sinx;
//acos(x)          arccosx;
//asin(x)          arcsinx;
//atan(x)          arctanx;
//fabs(x)          |x|;
//log(x)           lnx;
//log10(x)         lgx;
//sqrt(x)          x ^ (1 / 2);