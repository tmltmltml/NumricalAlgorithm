#include <cmath>
using namespace std;

double fun3(double x)
{
	double y = 12 * x * x - 16 * x + 2;
	return y;
}

//eg:
//C++              math
//exp(x)           e ^ x;
//pow(x, *)        x ^ *;
//cos(x)           cosx;
//sin(x)           sinx;
//acos(x)          arccosx;
//asin(x)          arcsinx;
//atan(x)          arctanx;
//fabs(x)          |x|;
//log(x)           lnx;
//log10(x)         lgx;
//sqrt(x)          x ^ (1 / 2);